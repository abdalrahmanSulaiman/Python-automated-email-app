name: automated email app
function: this app will use python to send an autemated email every day which includes
 news about the topic they selected and the information of the user will be stored in an
 excel file

 objects:
    excelfile:
        filepath
        get_data()
    email:
        sender
        receiver
        topic
        body
        send()
    news:
        data
        get()
